\doxysection{When}
\hypertarget{_when-example}{}\label{_when-example}A variable to enable/disable printing of the final result a user types the command "{}config"{} the parser actually generates and evaluates the parsing tree to 0.\+0 but this value should not be printed !

A variable to enable/disable printing of the final result a user types the command "{}config"{} the parser actually generates and evaluates the parsing tree to 0.\+0 but this value should not be printed !


\begin{DoxyCodeInclude}{0}

\end{DoxyCodeInclude}
 